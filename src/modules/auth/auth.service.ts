import { Injectable } from '@nestjs/common';

/**
 * AuthService
 * Handles registration, login and JWT token generation.
 * TODO: implement register(), login(), validateUser().
 */
@Injectable()
export class AuthService {}
