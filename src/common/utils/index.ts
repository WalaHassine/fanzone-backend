/**
 * Common utils barrel.
 *
 * Lets consumers import from '../../common/utils' rather than reaching into
 * individual files, matching `common/decorators` and `common/guards`.
 */
export { redactPath } from './redact-path.util';
